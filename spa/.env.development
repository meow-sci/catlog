# Empty on purpose. An empty base makes the API client emit origin-relative
# `/v1/…` URLs, which the Vite dev server proxies to catlogd (vite.config.ts).
# `pnpm dev` therefore exercises the app same-origin, with no dependency on the
# server's CORS allow-list being right.
VITE_CATLOG_API_BASE=
