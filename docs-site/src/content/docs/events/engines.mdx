---
title: Engines
description: >-
  Ignition, shutdown and flameout — all three read at whole-vehicle level, and
  two of the three feed a leaderboard.
sidebar:
  order: 5
---

import { Aside } from "@astrojs/starlight/components";
import EventDetail from "../../../components/EventDetail.astro";

Three events, all sampled twice a second, all read at **whole-vehicle** level
rather than per engine.

<Aside type="tip" title="All three can be switched off">
  They are optional in the mod's settings. Switching off ignition costs you Engines Lit, switching
  off running dry costs you Ran Dry, and switching off shutdown costs you nothing today — no board
  reads it. See [Turning things off](/catlog/start/turning-things-off/).
</Aside>

<Aside type="caution" title="Whole-vehicle means what it says">
catlog asks the game two questions about the vehicle: is *any* engine running, and
is *any* propellant available. It does not track engines individually. Shutting
down one of two engine groups records nothing at all — the event fires when the
last one stops.

The per-engine reading exists in the game but is not exposed in a form the mod can
read without reaching into internals it has no business in, so the whole-vehicle
answer is the honest one.

</Aside>

<EventDetail type="engine.ignition" />
<EventDetail type="engine.shutdown" />
<EventDetail type="engine.flameout" />

## Flameout is reconstructed

Kitten Space Agency has no concept called "flameout". What it has is, per engine,
whether that engine is active and whether propellant is available to it.

catlog rebuilds the idea from the two whole-vehicle answers: engines running,
propellant gone, and no start or stop this tick. That is a genuine
approximation — a vehicle with one tank dry and another still feeding will not
report a flameout — and it is recorded here rather than hidden.

## Two of the three feed a board

Ignition feeds **Engines Lit** and flameout feeds **Ran Dry**, one count each.
Shutdown is recorded and scores nothing: it is the unremarkable other half of
every burn, so counting it would just be counting ignitions again with a lag.

These three events were collected for a long time before anything read them, and
the boards were added afterwards, to a log that already held every ignition anyone
had ever shipped. That is the whole argument for keeping events you are not using
yet — and for [rebuilding](/catlog/leaderboards/eligibility/), which is what
turns that history into board rows.
