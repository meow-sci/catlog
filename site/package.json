{
  "name": "@meow-sci/catlog-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "catlog static assets (css/js) and playwright e2e suite. HTML is rendered by the Go server.",
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "e2e": "playwright test --config e2e/playwright.config.ts"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "5.3.0",
    "@playwright/test": "1.62.1",
    "esbuild": "0.28.1"
  }
}
