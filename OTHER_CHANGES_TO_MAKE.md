- This is stated in the docs-site: 

  > The name itself appears — “brought Hunter home safely” is the whole point of the feed. It is trimmed to 32 printable characters. The identifier beside it is not what you see.

  32 printable chars is TOO SHORT for the kitten name cutoff. make it 128 chars.
- introduce otel tracing in the code and run a local otel single-binary server. something super efficient.
- every page footer should have the total time to render and a sql query count (just for fun), if we can manage to track this accurately for a given datastar page render.  if we cant reliably count # of sql statement it required, just do the timing, use milliseconds as the unit.
- tone down the copy about privacy, we have it everywhere and it's unnecessary.  it only needs to be explained in a privacy notice style page, not on every page and footer and repeated elsewhere like it is now, this is just redundant.  players want to know this once and read about it on a privacy info page, but not see it all the time when just browsing the stats site (the main use case for catlog), so centralize it to a privacy statement page.
- 
- 
- 